/* A language is said to have first class functions when the functions in that language 
are treated as variables or normal values, you can save them, you can pass them as arguments to another 
function. */

// ex:
setTimeout(function(){})